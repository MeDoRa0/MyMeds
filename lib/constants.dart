import 'package:flutter/material.dart';

// for spalsh screen logo name
const kColorizeTextStyle = TextStyle(
  fontSize: 50.0,
  fontFamily: 'Horizon',
);
// Colorize Colors for text in splash screen
const kColorizeColors = [
  Colors.purple,
  Colors.blue,
  Colors.yellow,
  Colors.red,
];

const kPrimaryColor = Colors.yellow;
const kMedicineBox = 'medicine_box';
